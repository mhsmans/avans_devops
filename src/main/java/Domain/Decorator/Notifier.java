package Domain.Decorator;

public class Notifier {

    public void sendMessage(String message){
        // This method is empty because Notifier needs to be instantiated. This project's implementation of the
        // decorator pattern relies on it.
    };
}
